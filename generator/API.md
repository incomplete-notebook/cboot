# generator API 文档

本文档列出 `generator` 模块的所有公开 API。

## 目录

- [generate_project](#generate_project)
- [generate_module](#generate_module)
- [generate_mod_c](#generate_mod_c)
- [generate_mod_h](#generate_mod_h)
- [generate_mod_cmake](#generate_mod_cmake)
- [generate_top_cmake](#generate_top_cmake)
- [generate_top_main](#generate_top_main)

---

## 函数

### int generate_project()

生成完整项目代码

```c
int generate_project(struct Project* proj, char* output_dir)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `proj` | `struct Project*` | 项目定义 |
| `output_dir` | `char*` | 输出目录(项目根即为源码根) |

### void generate_module()

为单个模块生成代码

```c
void generate_module(struct Domain* mod, char* parent_dir)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `mod` | `struct Domain*` | 模块域 |
| `parent_dir` | `char*` | 父目录 |

### void generate_mod_c()

生成模块的.c源文件

```c
void generate_mod_c(struct Domain* mod, char* dir)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `mod` | `struct Domain*` | 模块域 |
| `dir` | `char*` | 模块目录路径 |

### void generate_mod_h()

生成模块的.h头文件(仅API声明)

```c
void generate_mod_h(struct Domain* mod, char* dir)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `mod` | `struct Domain*` | 模块域 |
| `dir` | `char*` | 模块目录路径 |

### void generate_mod_cmake()

生成模块的CMakeLists.txt

```c
void generate_mod_cmake(struct Domain* mod, char* dir)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `mod` | `struct Domain*` | 模块域 |
| `dir` | `char*` | 模块目录路径 |

### void generate_top_cmake()

生成顶层CMakeLists.txt

```c
void generate_top_cmake(struct Project* proj)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `proj` | `struct Project*` | 项目定义 |

### void generate_top_main()

生成顶层main.c入口

```c
void generate_top_main(struct Project* proj)
```

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `proj` | `struct Project*` | 项目定义 |

*Generated by CBoot v2.0*
