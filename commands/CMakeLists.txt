# CMakeLists.txt for module commands (CBoot generated, compiler: normal)

set(commands_SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/commands.c
)


# Export sources to parent scope
set(commands_SOURCES ${commands_SOURCES} PARENT_SCOPE)

