/* utils.h - CBoot generated (API declarations only) */
#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 确保目录存在，不存在则创建
int ensure_dir(char* path);

#endif /* UTILS_H */
