/* typecheck.h - CBoot generated (API declarations only) */
#ifndef TYPECHECK_H
#define TYPECHECK_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 检查单个域的类型一致性
int typecheck_domain(struct Domain* domain);

#endif /* TYPECHECK_H */
