# utils 开发文档

工具函数

## 函数

### char** tokenize()

**说明**: 分词

- **参数 `line`** (`char*`): 行
- **参数 `count`** (`int*`): 数量

### void free_tokens()

**说明**: 释放token

- **参数 `tokens`** (`char**`): token数组
- **参数 `count`** (`int`): 数量

### char* trim()

**说明**: 去空白

- **参数 `str`** (`char*`): 字符串

### void ensure_dir()

**说明**: 创建目录

- **参数 `path`** (`char*`): 路径

### int file_exists()

**说明**: 文件是否存在

- **参数 `path`** (`char*`): 路径

### int is_valid_identifier()

**说明**: 有效标识符

- **参数 `name`** (`char*`): 名称

### void strip_quotes()

**说明**: 去引号

- **参数 `str`** (`char*`): 字符串

*Generated by CBoot v2.0*
