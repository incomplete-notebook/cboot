# utils 完整文档

工具函数

## 目录

- [tokenize()](#tokenize)
- [free_tokens()](#free_tokens)
- [trim()](#trim)
- [ensure_dir()](#ensure_dir)
- [file_exists()](#file_exists)
- [is_valid_identifier()](#is_valid_identifier)
- [strip_quotes()](#strip_quotes)

---

## tokenize()

**声明**: `char** tokenize(char* line, int* count)`

**注释**: 分词

| 参数 | 类型 | 说明 |
|------|------|------|
| `line` | `char*` | 行 |
| `count` | `int*` | 数量 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## free_tokens()

**声明**: `void free_tokens(char** tokens, int count)`

**注释**: 释放token

| 参数 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | token数组 |
| `count` | `int` | 数量 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## trim()

**声明**: `char* trim(char* str)`

**注释**: 去空白

| 参数 | 类型 | 说明 |
|------|------|------|
| `str` | `char*` | 字符串 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## ensure_dir()

**声明**: `void ensure_dir(char* path)`

**注释**: 创建目录

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `char*` | 路径 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## file_exists()

**声明**: `int file_exists(char* path)`

**注释**: 文件是否存在

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `char*` | 路径 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## is_valid_identifier()

**声明**: `int is_valid_identifier(char* name)`

**注释**: 有效标识符

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

## strip_quotes()

**声明**: `void strip_quotes(char* str)`

**注释**: 去引号

| 参数 | 类型 | 说明 |
|------|------|------|
| `str` | `char*` | 字符串 |

**调用栈**: (此函数在模块 utils 中定义)

```c
//请在这里输入代码
```

---

*Generated by CBoot v2.0*
