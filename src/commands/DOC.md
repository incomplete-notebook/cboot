# commands 完整文档

命令处理器

## 目录

- [cmd_mod()](#cmd_mod)
- [cmd_struct()](#cmd_struct)
- [cmd_type()](#cmd_type)
- [cmd_def()](#cmd_def)
- [cmd_void()](#cmd_void)
- [cmd_var()](#cmd_var)
- [cmd_mem()](#cmd_mem)
- [cmd_enum()](#cmd_enum)
- [cmd_cmt()](#cmd_cmt)
- [cmd_value()](#cmd_value)
- [cmd_mode()](#cmd_mode)
- [cmd_cd()](#cmd_cd)
- [cmd_rm()](#cmd_rm)
- [cmd_find()](#cmd_find)
- [cmd_ls()](#cmd_ls)
- [cmd_mv()](#cmd_mv)
- [cmd_exit()](#cmd_exit)
- [cmd_gen()](#cmd_gen)
- [cmd_im()](#cmd_im)
- [cmd_res()](#cmd_res)

---

## cmd_mod()

**声明**: `int cmd_mod(char* name)`

**注释**: 创建模块

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_struct()

**声明**: `int cmd_struct(char* name)`

**注释**: 创建结构体

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_type()

**声明**: `int cmd_type(char* name)`

**注释**: 创建类型

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_def()

**声明**: `int cmd_def(char* name)`

**注释**: 创建宏

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_void()

**声明**: `int cmd_void(char* name, char* return_type)`

**注释**: 创建函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |
| `return_type` | `char*` | 返回类型 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_var()

**声明**: `int cmd_var(char* name, char* type)`

**注释**: 创建变量

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |
| `type` | `char*` | 类型 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_mem()

**声明**: `int cmd_mem(char* name, char* type)`

**注释**: 创建成员/参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |
| `type` | `char*` | 类型 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_enum()

**声明**: `int cmd_enum(char* defs, char* start_num)`

**注释**: 枚举创建宏

| 参数 | 类型 | 说明 |
|------|------|------|
| `defs` | `char*` | 定义 |
| `start_num` | `char*` | 起始编号 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_cmt()

**声明**: `int cmd_cmt(char* text)`

**注释**: 设置注释

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `char*` | 文本 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_value()

**声明**: `int cmd_value(char* text)`

**注释**: 设置值

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `char*` | 文本 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_mode()

**声明**: `int cmd_mode(char* text)`

**注释**: 设置模式

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `char*` | 文本 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_cd()

**声明**: `int cmd_cd(char* path)`

**注释**: 导航域树

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `char*` | 路径 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_rm()

**声明**: `int cmd_rm(char* name, int force)`

**注释**: 删除子域

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |
| `force` | `int` | 强制 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_find()

**声明**: `int cmd_find(char* type_filter, char* pattern, int flags)`

**注释**: 搜索域

| 参数 | 类型 | 说明 |
|------|------|------|
| `type_filter` | `char*` | 类型过滤 |
| `pattern` | `char*` | 模式 |
| `flags` | `int` | 标志 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_ls()

**声明**: `int cmd_ls(char* name)`

**注释**: 查看域

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 名称 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_mv()

**声明**: `int cmd_mv(char* src, char* target)`

**注释**: 移动域

| 参数 | 类型 | 说明 |
|------|------|------|
| `src` | `char*` | 源 |
| `target` | `char*` | 目标 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_exit()

**声明**: `int cmd_exit()`

**注释**: 退出

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_gen()

**声明**: `int cmd_gen()`

**注释**: 生成代码

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_im()

**声明**: `int cmd_im(char* path)`

**注释**: 导入脚本

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `char*` | 路径 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

## cmd_res()

**声明**: `int cmd_res(char* file_path)`

**注释**: 添加资源

| 参数 | 类型 | 说明 |
|------|------|------|
| `file_path` | `char*` | 文件路径 |

**调用栈**: (此函数在模块 commands 中定义)

```c
//请在这里输入代码
```

---

*Generated by CBoot v2.0*
