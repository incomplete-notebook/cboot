# typecheck 完整文档

类型检查器

## 目录

- [type_checker_init()](#type_checker_init)
- [type_checker_validate()](#type_checker_validate)
- [type_checker_is_builtin()](#type_checker_is_builtin)
- [type_checker_validate_value()](#type_checker_validate_value)
- [TypeChecker](#TypeChecker)

---

## type_checker_init()

**声明**: `void type_checker_init(TypeChecker* tc, Domain* scope)`

**注释**: 初始化

| 参数 | 类型 | 说明 |
|------|------|------|
| `tc` | `TypeChecker*` | 类型检查器 |
| `scope` | `Domain*` | 作用域 |

**调用栈**: (此函数在模块 typecheck 中定义)

```c
//请在这里输入代码
```

---

## type_checker_validate()

**声明**: `int type_checker_validate(TypeChecker* tc, char* type_name)`

**注释**: 验证类型

| 参数 | 类型 | 说明 |
|------|------|------|
| `tc` | `TypeChecker*` | 类型检查器 |
| `type_name` | `char*` | 类型名 |

**调用栈**: (此函数在模块 typecheck 中定义)

```c
//请在这里输入代码
```

---

## type_checker_is_builtin()

**声明**: `int type_checker_is_builtin(char* type_name)`

**注释**: 是否内置类型

| 参数 | 类型 | 说明 |
|------|------|------|
| `type_name` | `char*` | 类型名 |

**调用栈**: (此函数在模块 typecheck 中定义)

```c
//请在这里输入代码
```

---

## type_checker_validate_value()

**声明**: `int type_checker_validate_value(char* type_name, char* value)`

**注释**: 验证值

| 参数 | 类型 | 说明 |
|------|------|------|
| `type_name` | `char*` | 类型名 |
| `value` | `char*` | 值 |

**调用栈**: (此函数在模块 typecheck 中定义)

```c
//请在这里输入代码
```

---

## 定义

### struct TypeChecker

**注释**: 类型检查器

```c
typedef struct TypeChecker {
    Domain* scope;
    char* error_buf;
    int error_len;
} TypeChecker;
```

*Generated by CBoot v2.0*
