# typecheck API 文档

## API 函数

### void type_checker_init()

**说明**: 初始化

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tc` | `TypeChecker*` | 类型检查器 |
| `scope` | `Domain*` | 作用域 |

### int type_checker_validate()

**说明**: 验证类型

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tc` | `TypeChecker*` | 类型检查器 |
| `type_name` | `char*` | 类型名 |

### int type_checker_is_builtin()

**说明**: 是否内置类型

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `type_name` | `char*` | 类型名 |

### int type_checker_validate_value()

**说明**: 验证值

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `type_name` | `char*` | 类型名 |
| `value` | `char*` | 值 |

## API 类型

### TypeChecker

**说明**: 类型检查器

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `scope` | `Domain*` | 作用域 |
| `error_buf` | `char*` | 错误缓冲区 |
| `error_len` | `int` | 错误长度 |

*Generated by CBoot v2.0*
