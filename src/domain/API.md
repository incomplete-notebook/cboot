# domain API 文档

## API 函数

### Domain* domain_new()

**说明**: 创建新域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `type` | `DomainType` | 域类型 |
| `name` | `char*` | 名称 |
| `struct_size` | `size_t` | 结构体大小 |

### void domain_add_child()

**说明**: 添加子域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `parent` | `Domain*` | 父域 |
| `child` | `Domain*` | 子域 |

### void domain_delete()

**说明**: 删除域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `domain` | `Domain*` | 域 |

### void domain_remove_child()

**说明**: 移除子域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `parent` | `Domain*` | 父域 |
| `child` | `Domain*` | 子域 |

### Domain* domain_find_child()

**说明**: 查找子域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `parent` | `Domain*` | 父域 |
| `name` | `char*` | 名称 |

### Domain* domain_find_nearest_of_type()

**说明**: 向上查找最近类型域

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `from` | `Domain*` | 起始域 |
| `type` | `DomainType` | 域类型 |

### char* domain_get_path()

**说明**: 获取域路径

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `domain` | `Domain*` | 域 |

### void domain_set_comment()

**说明**: 设置注释

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `domain` | `Domain*` | 域 |
| `text` | `char*` | 文本 |

### void domain_set_value()

**说明**: 设置值

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `domain` | `Domain*` | 域 |
| `value` | `char*` | 值 |

### void domain_set_mode()

**说明**: 设置模式

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `domain` | `Domain*` | 域 |
| `mode` | `int` | 模式 |

### Project* project_new()

**说明**: 创建项目

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 项目名 |

### void project_free()

**说明**: 释放项目

**参数**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `proj` | `Project*` | 项目 |

## API 类型

### DomainType

**说明**: 域类型枚举

**底层类型**: `int`

### ModMode

**说明**: 模块模式: internal/external

**底层类型**: `int`

### ApiMode

**说明**: API模式: api/normal

**底层类型**: `int`

### TypeMode

**说明**: 类型模式: rename/struct/api rename/api struct

**底层类型**: `int`

### VarMode

**说明**: 变量模式: static/normal

**底层类型**: `int`

### RunMode

**说明**: 运行模式

**底层类型**: `int`

### Comment

**说明**: 注释

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `target` | `char*` | 目标 |
| `text` | `char*` | 文本 |

### Domain

**说明**: 域基类

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `type` | `DomainType` | 域类型 |
| `name` | `char*` | 名称 |
| `parent` | `Domain*` | 父域 |
| `children` | `Domain**` | 子域数组 |
| `child_count` | `int` | 子域数量 |
| `child_capacity` | `int` | 子域容量 |
| `comments` | `Comment*` | 注释数组 |
| `comment_count` | `int` | 注释数量 |
| `comment_capacity` | `int` | 注释容量 |
| `comment` | `char*` | 域注释 |

### ModuleDomain

**说明**: 模块域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `ModMode` | 模块模式 |
| `value` | `char*` | 值 |
| `includes` | `char**` | 包含文件数组 |
| `include_count` | `int` | 包含文件数量 |
| `include_capacity` | `int` | 包含文件容量 |
| `dependencies` | `char**` | 依赖数组 |
| `dep_count` | `int` | 依赖数量 |
| `dep_capacity` | `int` | 依赖容量 |

### FunctionDomain

**说明**: 函数域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `ApiMode` | API模式 |
| `return_type` | `char*` | 返回类型 |
| `code` | `char*` | 代码 |
| `value` | `char*` | 值 |

### StructDomain

**说明**: 结构体域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `ApiMode` | API模式 |

### TypeDomain

**说明**: 类型域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `TypeMode` | 类型模式 |
| `value` | `char*` | 值 |

### MacroDomain

**说明**: 宏域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `ApiMode` | API模式 |
| `value` | `char*` | 值 |

### VariableDomain

**说明**: 变量域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `mode` | `VarMode` | 变量模式 |
| `type` | `char*` | 类型 |
| `value` | `char*` | 值 |

### MemberDomain

**说明**: 成员域

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `base` | `Domain` | 基类 |
| `type` | `char*` | 类型 |

### Project

**说明**: 项目状态

**成员**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `name` | `char*` | 项目名 |
| `root` | `Domain*` | 根域 |
| `current` | `Domain*` | 当前域 |
| `has_generated` | `int` | 是否已生成 |
| `cboot_file` | `char*` | cboot文件 |
| `fine_tune_mode` | `int` | 微调模式 |

*Generated by CBoot v2.0*
