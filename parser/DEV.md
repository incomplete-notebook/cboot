# parser 开发文档

脚本解析器 - 解析.cboot脚本文件为项目定义

## 统计

- 公开 API: **3** 项
- 私有实现: **13** 项

## 函数

### int dispatch_script_line()

**业务逻辑**: 根据首个分词(命令名)调用对应cmd_函数

**说明**: 分词数量

```c
int dispatch_script_line(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### int is_cboot_ref()

```c
int is_cboot_ref(const char* token)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `token` | `const char*` | - |

---

### int exec_cboot_ref()

```c
int exec_cboot_ref(const char* ref)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `ref` | `const char*` | - |

---

### int try_cboot_ref()

> `API` — 公开接口

```c
int try_cboot_ref(const char* token)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `token` | `const char*` | - |

---

### int parse_cboot_script()

> `API` — 公开接口

```c
int parse_cboot_script(const char* filename)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `filename` | `const char*` | - |

---

### void join_tokens_from()

```c
void join_tokens_from(char** tokens, int count, int start, char* buf, size_t size)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |
| `start` | `int` | - |
| `buf` | `char*` | - |
| `size` | `size_t` | - |

---

### int require_args()

```c
int require_args(char** tokens, int count, int needed, const char* usage)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |
| `needed` | `int` | - |
| `usage` | `const char*` | - |

---

### int dispatch_build()

```c
int dispatch_build(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### int dispatch_modify()

```c
int dispatch_modify(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### int parse_rm()

```c
int parse_rm(char** tokens, int count, const char** name, int* force)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |
| `name` | `const char**` | - |
| `force` | `int*` | - |

---

### int parse_find_flags()

```c
int parse_find_flags(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### int dispatch_control()

```c
int dispatch_control(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### int dispatch_action()

```c
int dispatch_action(char** tokens, int count)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `tokens` | `char**` | - |
| `count` | `int` | - |

---

### void set_script_dir()

```c
void set_script_dir(const char* filename, char* saved_dir, size_t size)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `filename` | `const char*` | - |
| `saved_dir` | `char*` | - |
| `size` | `size_t` | - |

---

### int read_heredoc()

```c
int read_heredoc(FILE* f, int* line_no)
```

**参数列表**:

| 名称 | 类型 | 说明 |
|------|------|------|
| `f` | `FILE*` | - |
| `line_no` | `int*` | - |

---

## 宏

### `PARSER_NOT_HANDLED`

> `API` — 公开宏

```c
#define PARSER_NOT_HANDLED  ( - 1000 )
```


## 子模块

- [domain](domain/DEV.md): [API 引用] 从项目内模块 domain 导入

*Generated by CBoot v1.0.0*
